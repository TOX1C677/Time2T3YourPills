name: time2t3_your_pills
description: TimeToTake - клиент напоминаний о приёме лекарств (диплом), UI под ограничения моторики.
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.10.1

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  cupertino_icons: ^1.0.8
  provider: ^6.1.2
  get_storage: ^2.1.1
  dio: ^5.7.0
  flutter_secure_storage: ^9.2.2
  go_router: ^14.6.2
  connectivity_plus: ^6.1.1
  flutter_local_notifications: ^18.0.1
  timezone: ^0.9.4
  flutter_timezone: ^3.0.1
  uuid: ^4.5.1
  intl: ^0.20.2
  google_fonts: ^6.3.3
  permission_handler: ^11.3.1
  flutter_foreground_task: ^9.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
