# SQL-миграции (опционально)

Основной источник схемы — **SQLAlchemy** в `api/app/models.py` и миграции **Alembic** в `api/alembic/` (при добавлении).

Сюда можно выкладывать ручные `.sql` для аудита или восстановления на сервере без Alembic.
